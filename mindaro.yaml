workload: helm-release
properties:
  build:
    target: develop
  command: ["sleep", "infinity"]
  install:
    values:
    - .secrets.yaml
  sync:
    enabled: true
  tracing: true
  routing:
    toMainline: true
    toSideline: true
  skipHealthChecks: true
  killOnTerminate: true